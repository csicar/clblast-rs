[CLBlast](https://github.com/CNugteren/CLBlast) Bindings for Rust
===========================================================

There are two crates:

`clblast-sys`
-------------

basic bindings to clblast with types from `cl-sys` on the rust side.


`clblast`
---------

high level bindings to clblast based on the `ocl` crate.